% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/initialize.R
\name{initialize_app}
\alias{initialize_app}
\title{Initialize presentifyr Application Environment}
\usage{
initialize_app(project_dir = get_project_dir(), verbose = TRUE)
}
\arguments{
\item{project_dir}{The file path to the main project directory.
Must already exist. Default is \code{get_project_dir()}.}

\item{verbose}{Logical. Print detailed initialization messages.
Default is \code{TRUE}.}
}
\value{
Invisibly returns a list with initialization status:
\itemize{
\item \code{venv}: Logical indicating \verb{.venv/} exists after the call
\item \code{python_pptx}: Logical indicating python-pptx is importable
\item \code{errors}: Character vector of any errors encountered
}
}
\description{
Bootstraps the Python environment presentifyr's Shiny app needs:
calls \code{fyrstartr::initialize_python(groups = "presentifyr")} to
install uv (if missing) and additively sync the \code{presentifyr}
dependency group (python-pptx, pillow) into \verb{.venv/} from the
fyrstartr-bundled lockfile. The sync runs in \code{--inexact} mode, so
any packages already present in the venv from prior fyr-package
installs are left in place.
}
\examples{
\dontrun{
initialize_app()
}
}
